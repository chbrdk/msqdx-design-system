# Dependencies
node_modules

# Build output
dist
*.tsbuildinfo

# Storybook
storybook-static

# OS
.DS_Store
Thumbs.db

# IDE / misc
.specstory
*.log
.env
.env.local
.env.*.local

# Backup / copy folders (do not publish)
packages copy/
packages copy 2/

# Optional: uncomment to avoid committing lockfile (some prefer committing it for reproducibility)
# package-lock.json
